function translateText(spanishText, englishText) {

  // const userLanguage = cookies.get('langKey') ?? 'en';
  // return userLanguage === 'ar' ? arabicText : englishText;

  return spanishText
}


export const CLAIM_TYPE_REQUIRED = translateText("El nombre del tipo de reclamo es obligatorio.","Name of claim type is required.")